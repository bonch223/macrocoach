rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow all read/write access for debugging
    // TODO: Restrict permissions once app is working
    match /{document=**} {
      allow read, write: if true;
    }
  }
}